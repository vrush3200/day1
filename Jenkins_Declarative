pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                git credentialsID: 'github' , url: 'https://github.com/vrush3200/day1.git'
            }
        }
    }
}
